export default function ({count}){
    return (
        <div>
            <h1>the count is {count}</h1>
            <h1>the count is {count}</h1>
            <h1>the count is {count}</h1>
        </div>
    )
}